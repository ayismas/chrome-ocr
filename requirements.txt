# Runtime dependencies for chrome-ocr
# Install with: pip install -r requirements.txt

# Core — required for image_to_text()
numpy>=1.23
pillow>=9.0
protobuf>=4.0

# Optional — required only for pdf_to_markdown()
# pymupdf>=1.22
